// Assembly-level metadata for Acme.Orders.
//
// This file declares no type at all, which is the point: it is the fixture's only exercise of the
// `title` rule's last tier, where derivation falls back to the file name and emits a warning.

using System.Reflection;

[assembly: AssemblyMetadata("Fixture", "csharp2md")]
[assembly: AssemblyMetadata("Purpose", "title-fallback-coverage")]

namespace Acme.Shipping;

/// <summary>Composition root so the project compiles as an application without extra surface.</summary>
public static class Program
{
    public static void Main(string[] args)
    {
        _ = args;
    }
}

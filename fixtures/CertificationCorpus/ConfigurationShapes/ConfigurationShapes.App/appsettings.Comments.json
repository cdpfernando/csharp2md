{
  // GCPC-105/F2: the .NET configuration provider accepts line comments; JsonDocument.Parse does not.
  "Certification": {
    /* block comment, also accepted by the .NET configuration provider */
    "Level": "Info"
  }
}
